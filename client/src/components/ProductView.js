// isolated component or create a reserved reducer for "opened product view" ?
import React from "react";

export default function (props) {
    return (
        <div>
            <h3>Product view</h3>
        </div>
    );
}